Apply a final polish pass to the updated ReconCore desktop design. Do not redesign from scratch. Keep the current enterprise desktop visual language, the left navigation, the inspector panels, the high-density tables, and the overall JavaFX/FXML-friendly layout.

The current update is much closer to the business requirements. This final pass should focus on:
1. removing contradictory old screens,
2. fixing remaining domain mismatches,
3. improving business accuracy in sample data,
4. completing missing table semantics,
5. making the design package consistent enough for implementation handoff.

Preserve these updated screens and their general structure:
- corrected dashboard
- input/validation
- processing configuration
- execution progress
- product mapping review
- unit mismatch review
- detailed results
- negative inventory
- export
- run history
- settings

Now make the following corrections.

1. Consolidate the package and remove conflicting legacy screens
The package still contains older conflicting screens such as:
- `reconcore_main_workspace`
- `reconcore_data_validation`
- `reconcore_workspace_overview`
- `reconcore_detailed_reconciliation_table`

These use old concepts like:
- generic file staging with 3 independent files
- CSV/XML demo language
- `Run System`
- generic SaaS-like summary cards

The final design package should expose only one coherent product direction. Archive or remove obsolete screen concepts from the handoff set, and clearly mark the final canonical screens.

2. Use the corrected dashboard as the canonical workspace screen
Keep the corrected dashboard structure, not the older workspace overview.
The canonical workspace must show:
- XNT source
- invoice source
- detected sheets for the invoice workbook:
  - `MuaVao_1`
  - `BanRa_1`
- validation warnings
- mapping review backlog
- unit mismatch queue
- negative inventory count
- current period
- last run state

3. Replace remaining generic/non-domain sample data
Several screens still use unrelated demo content such as:
- MacBook / Dell / Logitech
- GL Extract / POS Transactions
- vendor_a.csv
- generic enterprise software examples

Replace these with inventory/tax reconciliation examples aligned with the real product domain.

Use examples such as:
- `Tôn màu`
- `Tôn xốp`
- `Thép ống hộp`
- `Đầu nổ robin (EH0930)`
- `Máy xịt rửa`
- material or stock items with dimensions, gauges, weights, and model numbers

Also use realistic unit examples:
- `m`
- `kg`
- `cái`
- `thùng`
- `bao`
- `cây`
- `cuộn`

And realistic mismatch examples:
- `kg` vs `cây`
- `m` vs `cuộn`
- `tấn` vs `bao`

4. Update the Product Mapping Review screen to match the real business rules
Keep the current layout, but replace consumer-electronics examples with inventory/material examples.

The examples must demonstrate:
- strong numeric/specification tokens
- dimension/model-sensitive matching
- unit mismatch warnings
- items present in invoice data but not in XNT

Add or make explicit:
- a `Warnings` column that is visible and meaningful
- a visible `Not in XNT` state
- an `Ignore Warning` action
- a `Search XNT Catalog` action or manual lookup affordance
- a sticky bulk action bar for multi-row confirmation

In the inspector panel, make sure the extracted tokens reflect real inventory matching logic:
- size
- thickness
- diameter
- length
- model code
- weight

5. Strengthen the Detailed Results screen so the report structure is obvious
The current screen direction is good, but the final design must make it obvious that the report includes both quantity and amount comparisons.

Ensure the table semantics visibly cover:
- standardized product name
- XNT product name
- invoice product name
- XNT unit
- invoice unit
- match status
- warnings
- opening quantity
- opening amount
- purchase quantity
- purchase amount
- inbound quantity from XNT
- inbound amount from XNT
- purchase vs inbound quantity difference
- purchase vs inbound amount difference
- sales quantity
- sales amount
- outbound quantity from XNT
- outbound amount from XNT
- sales vs outbound quantity difference
- sales vs outbound amount difference
- calculated ending quantity
- calculated ending amount
- XNT ending quantity
- XNT ending amount
- ending quantity difference
- ending amount difference

If all columns cannot be shown at once, the design must clearly indicate:
- frozen first columns
- horizontal scrolling
- column visibility management
- table density controls

6. Improve the Negative Inventory screen to fully reflect review context
Keep the current screen layout, but add:
- match status column
- warnings column
- explicit `Exists in XNT` or `Invoice-only` indicator
- clearer same-day sales amount label
- more domain-accurate sample item names

The inspector should explain the negative inventory event using the same business logic language:
- opening quantity
- cumulative purchase quantity
- cumulative sales quantity
- resulting negative quantity

7. Make Execution Progress and Run History domain-consistent
The screen structure is good, but the log content is still too generic.

Replace generic examples like:
- `vendor_a.csv`
- `GL_Extract_Q3.csv`
- `POS_Transactions_Prod`

with domain-consistent examples like:
- `xnt.xlsx`
- `Invoices_Jan.xlsx`
- `MuaVao_1`
- `BanRa_1`
- `name normalization`
- `unit mismatch detection`
- `negative inventory analysis`

8. Improve Settings so it reflects the actual reconciliation rules
The settings screen is structurally good, but add or make explicit:
- unit mismatch warning behavior
- rule for products not found in XNT
- AI confidence threshold
- auto-accept vs manual review
- normalization toggles relevant to product names:
  - case-insensitive matching
  - trim whitespace
  - accent/diacritic normalization if appropriate
  - punctuation normalization
  - numeric token prioritization

9. Improve Input/Validation sample data to reflect real XNT/invoice structure
The improved input screen already has the right flow. Keep it.
But the sample table content should look closer to real inventory reconciliation:
- item names and units from stock/invoice contexts
- preview tabs for `XNT`, `MuaVao_1`, `BanRa_1`
- issue examples like:
  - invalid date
  - null tax id
  - negative amount
  - unit anomaly

10. Final handoff requirement
The final design handoff must be implementation-oriented.

For each canonical screen, provide:
- screen name
- purpose
- layout structure
- key components
- main actions
- important data states
- notes for JavaFX/FXML implementation

The goal of this pass is not new invention. The goal is to make the existing updated design package:
- consistent
- domain-correct
- free of contradictory old screens
- ready for UI implementation planning
